[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4.6", features = ["derive"] }
client = { path = "../client" }
env_logger = "0.10.0"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
