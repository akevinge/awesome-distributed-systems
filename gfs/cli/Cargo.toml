[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4.6", features = ["derive"] }
client = { path = "../client" }
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
